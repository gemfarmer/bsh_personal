// Generated by CoffeeScript 1.6.3
(function() {
  $(function() {
    var $contactForm, $tab;
    $('#myTab a').on('click', function(e) {
      e.preventDefault();
      return $(this).tab('show');
    });
    $('#myTab a[href="#profile"]').tab('show');
    $('#myTab a:first').tab('show');
    $('#myTab a:last').tab('show');
    $('#myTab li:eq(2) a').tab('show');
    $tab = $('.nav-tab li');
    $contactForm = $('#contact-form');
    console.log($contactForm);
    return $contactForm.on('submit', function(event) {
      "click";
      var info;
      event.preventDefault();
      info = $contactForm.serialize();
      $contactForm.each(function() {
        return this.reset();
      });
      return $.post('/sendemail', info, function(err, data) {
        if (data['success']) {
          return $('#success-message').removeClass('hidden');
        } else if (data['failure']) {
          return $('#success-message').removeClass.html("Please Complete the Form");
        }
      });
    });
  });

}).call(this);
